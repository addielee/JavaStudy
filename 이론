피처폰과 스마트폰의 차이 : 운영체제의 유무
앱->웹(브라우저)로 가는 추세 : 무제한 데이터, 와이파이 등의 보편화 -> 반응형 웹 개발

자바 특징 : 운영체제 독립적, 객체지향

JDK(자바 개발 도구, 컴파일) : JRE(실행만 가능) > API(만들어져 있는 것 가져와 사용 가능) > JVM(운영체제에 독립적, 메모리 관리) 를 포함

통합 개발환경(IDE Tool) : ex) 이클립스

모든 객체는 속성(재료 개념)과 기능(메소드)를 갖고 있음
-속성=Field=멤버변수
-기능=method=함수

1. 컴파일 : 자바는 컴파일 언어면서 인터프리터 언어. 오류가 순서대로 뜸
-문법적 오류가 있는지 확인
cf)인터프리터 언어: 컴파일은 문서 전체 다 본 뒤에 오류 개수 알려줌. 인터프리터는 한 줄 확인 후 바로 오류 확인
-컴파일은 코딩 순서(실행은 코딩 순서가 아니라 호출 순서)

컴파일 확인 방법
1) javac(.exe) 파일 찾기
2) javac 파일이름.java
자동으로 경로 잡을 수 있도록 환경설정(path) -> 고정적인 javac.exe로 path 잡기
3)컴파일 성공하면 ~.class 생성(byte코드로 변환)->~.class가 실행 파일

2.실행
java(.exe) 
